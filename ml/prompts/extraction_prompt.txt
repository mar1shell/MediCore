You are a medical entity extraction system. Given the following patient text, extract all:
- Allergies (drugs, foods, environmental)
- Current medications (include dose if mentioned)
- Active diagnoses / conditions

Return ONLY valid JSON matching this schema:
{
  "allergies": ["..."],
  "medications": ["..."],
  "diagnosis": ["..."]
}

Do not include any explanation. If a category is not mentioned, return an empty array.

Patient text:
{text}
